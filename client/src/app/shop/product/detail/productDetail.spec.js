describe( 'shop product detail section', function() {
  beforeEach(module('meanShop.shop.productDetail'));

  it('should have a dummy test', inject( function() {
    expect(true).toBeTruthy();
  }));
});

