describe( 'shop product list section', function() {
  beforeEach(module('meanShop.shop.productList'));

  it('should have a dummy test', inject( function() {
    expect(true).toBeTruthy();
  }));
});

