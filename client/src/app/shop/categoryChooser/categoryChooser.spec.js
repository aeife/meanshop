describe( 'category chooser section', function() {
  beforeEach(module('meanShop.shop.shopCategory'));

  it('should have a dummy test', inject( function() {
    expect(true).toBeTruthy();
  }));
});
